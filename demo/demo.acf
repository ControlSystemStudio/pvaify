# Access control configuration

# IOC will allow write access to any PV
# but briefly change them to read-only,
# simulating a machine where PVs are
# sometimes read-only, for example while
# in a certain machine mode
ASG (DEFAULT)
{
  INPA(demo:ai1)
  RULE(1, READ)
  RULE(1, WRITE)
  {
    CALC(A<8)
  }  
}

ASG (ALWAYS)
{
  RULE(1, READ)
  RULE(1, WRITE)
}